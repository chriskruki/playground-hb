---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - score/package.json
  - score/tsconfig.json
  - score/next.config.ts
  - score/postcss.config.mjs
  - score/components.json
  - score/tailwind.config.ts
  - score/src/app/globals.css
  - score/src/app/layout.tsx
  - score/src/app/page.tsx
  - score/src/lib/utils.ts
  - score/src/lib/theme.ts
  - score/src/components/ui/button.tsx
  - score/src/components/ui/card.tsx
  - score/src/components/ui/input.tsx
  - score/src/components/app-shell.tsx
  - score/src/components/logo.tsx
  - score/public/logo.svg
autonomous: true
requirements:
  - TECH-01
  - THEME-01
  - THEME-02
  - THEME-03

must_haves:
  truths:
    - "App colors can be changed by editing a single theme config file with no other code changes required"
    - "Logo can be swapped by replacing logo.svg with no code changes"
    - "App renders correctly on a 390px wide mobile screen and does not break on desktop"
    - "Theming uses Tailwind CSS custom properties — no hardcoded color values in components"
  artifacts:
    - path: "score/src/app/globals.css"
      provides: "Theme CSS custom properties (background, primary, secondary, accent, card, border, ring, destructive) sourced from theme config values"
      contains: "--background"
    - path: "score/src/lib/theme.ts"
      provides: "Single source of truth for brand colors — the ONE file to edit to change app colors"
      exports: ["themeColors"]
    - path: "score/src/components/logo.tsx"
      provides: "Logo component loading from public/logo.svg"
      contains: "logo.svg"
    - path: "score/public/logo.svg"
      provides: "Swappable logo asset"
    - path: "score/src/app/layout.tsx"
      provides: "Root layout with Bricolage Grotesque font, viewport meta for mobile"
      contains: "Bricolage_Grotesque"
    - path: "score/src/components/app-shell.tsx"
      provides: "Mobile-first responsive shell with max-width container and logo header"
      min_lines: 15
  key_links:
    - from: "score/src/app/globals.css"
      to: "score/src/lib/theme.ts"
      via: "CSS custom properties values match theme config"
      pattern: "--primary|--background|--secondary"
    - from: "score/src/components/logo.tsx"
      to: "score/public/logo.svg"
      via: "Image src reference"
      pattern: "logo\\.svg"
    - from: "score/src/app/layout.tsx"
      to: "score/src/app/globals.css"
      via: "CSS import in layout"
      pattern: "globals\\.css"
---

<objective>
Scaffold the Next.js project from scratch and establish the theming system so that all future components inherit consistent brand styling from a single configuration source. Download the Playground HB logo as a local asset and set up Bricolage Grotesque as the app font.

Purpose: Every component built in later phases must use theme tokens (not hardcoded colors). Getting this right now prevents color-hunting refactors later. The logo and font establish the Playground HB brand identity from day one.

Output: A buildable Next.js app in `score/` with Tailwind v4 theming, shadcn UI base components, mobile-first responsive shell, and brand assets.
</objective>

<execution_context>
@/Users/chriskruki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskruki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md

Reference prototype for patterns (do NOT copy code):
@score_proto/src/app/globals.css (theme variable structure)
@score_proto/package.json (dependency list)
@score_proto/src/components/ui/button.tsx (shadcn component pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project with Tailwind v4 theming and brand assets</name>
  <files>
    score/package.json
    score/tsconfig.json
    score/next.config.ts
    score/postcss.config.mjs
    score/src/app/globals.css
    score/src/app/layout.tsx
    score/src/app/page.tsx
    score/src/lib/utils.ts
    score/src/lib/theme.ts
    score/public/logo.svg
    score/components.json
  </files>
  <action>
    Create a new Next.js project in `score/` directory (NOT inside score_proto — this is a fresh build).

    1. **Initialize project manually** (do not use `create-next-app` — create files directly):
       - `package.json` with Next.js 16, React 19, Tailwind v4, `@tailwindcss/postcss`, `tw-animate-css`, `framer-motion`, `class-variance-authority`, `clsx`, `tailwind-merge`, `lucide-react`, `zustand` (will be used in plan 02). TypeScript 5 and `@types/react`, `@types/react-dom` as devDependencies.
       - Scripts: `dev` (next dev --turbopack), `build` (next build), `start` (next start), `lint` (next lint)
       - `tsconfig.json` with path alias `@/*` -> `src/*`, strict mode, bundler module resolution
       - `next.config.ts` — minimal config, no special settings needed
       - `postcss.config.mjs` with `@tailwindcss/postcss` plugin
       - `.gitignore` for Next.js (node_modules, .next, etc.)

    2. **Create theme config** (`src/lib/theme.ts`):
       - Export a `themeColors` object as the single source of truth for all brand colors:
         ```
         background: '#FFF2D4'      // warm yellow
         foreground: 'oklch(0.145 0 0)'
         card: 'oklch(1 0 0)'       // white
         cardForeground: 'oklch(0.145 0 0)'
         primary: '#34654A'          // forest green
         primaryForeground: 'oklch(0.98 0 0)'
         secondary: '#DCAF56'        // golden yellow
         secondaryForeground: 'oklch(0.1 0 0)'
         accent: '#DCAF56'
         accentForeground: 'oklch(0.1 0 0)'
         muted: 'oklch(0.97 0 0)'
         mutedForeground: 'oklch(0.556 0 0)'
         destructive: 'oklch(0.577 0.245 27.325)'
         border: 'oklch(0.85 0.02 85)'
         input: 'oklch(0.95 0.02 85)'
         ring: '#34654A'
         radius: '0.625rem'
         ```
       - This file is the ONE place to change app colors. Document this in a comment at the top.

    3. **Create globals.css** with Tailwind v4 structure:
       - `@import "tailwindcss"` and `@import "tw-animate-css"`
       - `@theme inline` block mapping CSS custom properties to Tailwind color names (same pattern as prototype's globals.css)
       - `:root` block setting CSS custom properties from the theme values
       - Base layer styles: border-border, outline-ring, bg-background, text-foreground
       - Mobile input zoom prevention (`font-size: 16px` on inputs for iOS)
       - IMPORTANT: The CSS custom property values must match theme.ts exactly. While globals.css contains the actual CSS, theme.ts serves as the documented source of truth and is importable by any component that needs color values programmatically.

    4. **Create layout.tsx**:
       - Import Bricolage Grotesque from `next/font/google` as the single font family
       - Set font variable `--font-bricolage` and apply to body
       - Metadata: title "Playground HB Score", description "Mini golf scorecard for Playground HB"
       - Viewport meta: `width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no` (prevents pinch-zoom on scoring inputs)
       - Apply font class and `antialiased` to body

    5. **Create page.tsx**: Minimal placeholder — just import and render `<AppShell />` (created in Task 2). Add `"use client"` directive since AppShell will use state.

    6. **Download logo**: Use curl to download from `https://playgroundhb.com/cdn/shop/files/playground_logo_only_black_copy.png` and save to `score/public/logo.png`. Then create a simple `score/public/logo.svg` that references or wraps this (or if the download fails, create a clean SVG text placeholder saying "Playground HB"). The key: components reference `logo.svg` — swapping means replacing one file.

    7. **Create utils.ts**: Standard shadcn cn() utility using clsx + tailwind-merge.

    8. **Set up shadcn base components**: Create `components.json` for shadcn config. Manually create `button.tsx`, `card.tsx`, and `input.tsx` in `src/components/ui/` following shadcn patterns but using the project's theme variables. These are the three components needed for the foundation shell.

    9. Run `cd score && npm install` to install all dependencies.

    10. Run `cd score && npm run build` to verify the project compiles.
  </action>
  <verify>
    <automated>cd /Users/chriskruki/gitlab/playground-hb/score && npm run build</automated>
    <manual>Check that score/src/lib/theme.ts exists and exports themeColors with all brand colors</manual>
  </verify>
  <done>
    - `score/` directory contains a buildable Next.js 16 project
    - `src/lib/theme.ts` exports themeColors as single source of truth for all brand colors
    - `globals.css` contains CSS custom properties matching theme config values
    - Bricolage Grotesque font loaded in layout.tsx
    - `public/logo.svg` exists as a swappable asset
    - `npm run build` succeeds with zero errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create mobile-first app shell with logo header and responsive container</name>
  <files>
    score/src/components/app-shell.tsx
    score/src/components/logo.tsx
  </files>
  <action>
    1. **Create Logo component** (`src/components/logo.tsx`):
       - Renders `<img src="/logo.svg" alt="Playground HB" />` — no CDN URL, no fallback logic (just the local file)
       - Accept optional `className` prop for sizing flexibility
       - Default height: `h-10` (40px) with `w-auto`
       - This is the only place logo.svg is referenced — swapping the file changes the logo everywhere

    2. **Create AppShell component** (`src/components/app-shell.tsx`):
       - `"use client"` directive (will house state machine in Plan 02)
       - Mobile-first responsive container: `max-w-md mx-auto px-4` (max 448px — iPhone-class width with padding)
       - Sticky header with logo centered, subtle bottom border using theme border color, backdrop blur for scroll-under effect
       - Main content area below header with vertical padding
       - Full viewport height: `min-h-screen` with `bg-background`
       - For now, render a placeholder in the content area: a centered heading "Playground HB Score" in primary color with a subtitle "Foundation ready" — this proves the theme is working

    3. **Verify mobile rendering**:
       - All content stays within the max-w-md container
       - No horizontal overflow at 390px viewport width
       - Header sticks on scroll
       - Theme colors render correctly (warm yellow background, green text for primary elements, white card if present)
  </action>
  <verify>
    <automated>cd /Users/chriskruki/gitlab/playground-hb/score && npm run build && echo "Build OK"</automated>
    <manual>Run `npm run dev` in score/, open http://localhost:3000 on mobile viewport (390px), verify warm yellow background, centered logo, green heading text, no horizontal scroll</manual>
  </verify>
  <done>
    - AppShell renders with sticky logo header and responsive container
    - Logo component loads from /logo.svg with no hardcoded CDN URLs
    - Layout is mobile-first: content fits 390px width, scales to desktop without breaking
    - Theme colors visually match: warm yellow background (#FFF2D4), forest green primary (#34654A), white cards
    - Bricolage Grotesque font is visible in rendered text
  </done>
</task>

</tasks>

<verification>
1. `cd score && npm run build` — must succeed with zero errors
2. `score/src/lib/theme.ts` exists and exports color values matching CONTEXT.md palette exactly
3. `score/src/app/globals.css` CSS custom properties reference the same color values as theme.ts
4. `score/public/logo.svg` exists — the only logo asset referenced by components
5. No hardcoded color hex values in any component file (only theme token classes like `text-primary`, `bg-background`)
6. `score/src/app/layout.tsx` loads Bricolage Grotesque and sets viewport meta preventing mobile zoom
</verification>

<success_criteria>
- The project builds successfully with `npm run build`
- Changing a color in theme.ts (and its corresponding CSS variable in globals.css) changes the color everywhere — no other files need editing
- Replacing public/logo.svg changes the logo everywhere — no code changes needed
- On a 390px viewport, content fits without horizontal scroll and looks intentional (not just "not broken")
- On desktop, content is centered and readable (max-width container)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
