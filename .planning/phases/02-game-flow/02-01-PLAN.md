---
phase: 02-game-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - score/src/content/holes/hole-01.md
  - score/src/content/holes/hole-02.md
  - score/src/content/holes/hole-03.md
  - score/src/content/holes/hole-04.md
  - score/src/content/holes/hole-05.md
  - score/src/content/holes/hole-06.md
  - score/src/content/holes/hole-07.md
  - score/src/content/holes/hole-08.md
  - score/src/content/holes/hole-09.md
  - score/src/lib/holes.ts
  - score/src/app/page.tsx
  - score/src/components/app-shell.tsx
  - score/src/components/landing-screen.tsx
  - score/src/components/setup-screen.tsx
autonomous: true
requirements: [LAND-01, LAND-02, SETUP-01, SETUP-02, SETUP-03, SETUP-04, HOLE-02, HOLE-03]

must_haves:
  truths:
    - "User sees branded landing page with Playground HB logo, course name, and a prominent Start Game button"
    - "User can add player names one at a time and remove them before starting"
    - "User cannot start game with zero players or players with empty names"
    - "User sees animated player list (items enter/exit with animation)"
    - "All 9 holes have markdown files with name, par, and instructions"
    - "Hole data loads server-side and passes to client AppShell as a prop"
  artifacts:
    - path: "score/src/components/landing-screen.tsx"
      provides: "Branded landing page with logo and Start Game"
      exports: ["LandingScreen"]
    - path: "score/src/components/setup-screen.tsx"
      provides: "Player name management with add/remove and validation"
      exports: ["SetupScreen"]
    - path: "score/src/lib/holes.ts"
      provides: "HoleData type and server-side hole loading from markdown"
      exports: ["HoleData", "holes", "loadHoles"]
    - path: "score/src/content/holes/hole-01.md"
      provides: "Hole 1 markdown content"
      contains: "name:"
    - path: "score/src/content/holes/hole-09.md"
      provides: "Hole 9 markdown content"
      contains: "name:"
  key_links:
    - from: "score/src/app/page.tsx"
      to: "score/src/lib/holes.ts"
      via: "server-side import of holes array"
      pattern: "import.*holes.*from.*lib/holes"
    - from: "score/src/app/page.tsx"
      to: "score/src/components/app-shell.tsx"
      via: "passes holesData prop"
      pattern: "holesData=\\{holes\\}"
    - from: "score/src/components/app-shell.tsx"
      to: "score/src/components/landing-screen.tsx"
      via: "renders LandingScreen in phase=landing"
      pattern: "LandingScreen"
    - from: "score/src/components/app-shell.tsx"
      to: "score/src/components/setup-screen.tsx"
      via: "renders SetupScreen in phase=setup"
      pattern: "SetupScreen"
---

<objective>
Build the hole content data layer, branded landing screen, and animated player setup screen -- everything a player sees before scoring begins.

Purpose: Establishes the server-side hole data pipeline (gray-matter markdown files passed as props to client AppShell) and replaces the two placeholder screens (landing, setup) with full implementations. This is the prerequisite for the scoring screen in Plan 02.

Output: 9 hole markdown files, holes.ts data loader, refactored page.tsx (server component), new LandingScreen and SetupScreen components, updated AppShell accepting holesData prop.
</objective>

<execution_context>
@/Users/chriskruki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskruki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-game-flow/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From score/src/lib/game-store.ts:
```typescript
export type GamePhase = "landing" | "setup" | "scoring" | "results";

export interface Player {
  id: string;
  name: string;
}

// Store actions used by landing/setup:
goToSetup: () => void;          // landing -> setup (guards phase === "landing")
goToScoring: () => void;        // setup -> scoring (guards phase === "setup", validates players)
addPlayer: (name: string) => void;
removePlayer: (id: string) => void;
updatePlayerName: (id: string, name: string) => void;
```

From score/src/components/ui/button.tsx:
```typescript
// Variants: default, destructive, outline, secondary, ghost, link
// Sizes: default, sm, lg, icon (all have min-h-[44px] for touch)
export { Button, buttonVariants };
```

From score/src/components/ui/input.tsx:
```typescript
export { Input };
// Standard input with theme border/focus styling
```

From score/src/components/ui/card.tsx:
```typescript
export { Card, CardHeader, CardFooter, CardTitle, CardDescription, CardContent, CardAction };
```

From score/src/components/logo.tsx:
```typescript
export function Logo(): JSX.Element;
// Renders /logo.svg, 40px height
```

From score/src/app/page.tsx (CURRENT -- needs refactor):
```typescript
"use client";
import { AppShell } from "@/components/app-shell";
export default function Home() {
  return <AppShell />;
}
```

From score/src/components/app-shell.tsx (CURRENT -- needs refactor):
```typescript
export function AppShell(): JSX.Element;
// Currently contains inline LandingScreen, SetupScreen, ScoringScreen, ResultsScreen
// Has: hydration guard, back-button interception, AnimatePresence phase transitions
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hole content data layer with gray-matter and refactor page.tsx for server-side loading</name>
  <files>score/src/content/holes/hole-01.md, score/src/content/holes/hole-02.md, score/src/content/holes/hole-03.md, score/src/content/holes/hole-04.md, score/src/content/holes/hole-05.md, score/src/content/holes/hole-06.md, score/src/content/holes/hole-07.md, score/src/content/holes/hole-08.md, score/src/content/holes/hole-09.md, score/src/lib/holes.ts, score/src/app/page.tsx, score/src/components/app-shell.tsx</files>
  <action>
  1. Install gray-matter: `cd score && npm install gray-matter`

  2. Create 9 markdown files in `score/src/content/holes/` with this frontmatter format:
     ```
     ---
     name: "[hole name]"
     par: 3
     illustration: "/illustrations/hole-[NN].svg"
     ---
     [1-2 sentence instruction text]
     ```
     Use these hole names (fun mini golf theme):
     - hole-01: "Pay-Lay Your Bets" (par 3) -- "Aim for the center ramp to avoid the side gutters. A steady hand wins here!"
     - hole-02: "The Windmill" (par 3) -- "Time your shot to slip past the spinning blades. Patience is key!"
     - hole-03: "Loop de Loop" (par 4) -- "Send it up the loop with enough speed to make it around. Too slow and it rolls back!"
     - hole-04: "The Volcano" (par 3) -- "Aim straight for the crater. The ball drops through to the other side!"
     - hole-05: "Pirate's Cove" (par 4) -- "Navigate around the ship and through the cave. Watch for the curve at the end!"
     - hole-06: "The Bridge" (par 3) -- "A straight shot over the bridge. Don't let the height psych you out!"
     - hole-07: "Zig Zag Alley" (par 4) -- "Bounce your way through the zig-zag walls. Angles are everything!"
     - hole-08: "The Tunnel" (par 3) -- "Roll it through the dark tunnel. Trust your aim, you can't see the other side!"
     - hole-09: "The Grand Finale" (par 5) -- "The longest hole on the course. Multiple obstacles stand between you and glory!"

  3. Create `score/src/lib/holes.ts`:
     - Export `HoleData` interface: `{ number: number; name: string; par: number; illustration: string; instructions: string; }`
     - Export `loadHoles()` function that reads all 9 markdown files from `src/content/holes/` using `fs.readFileSync` + `gray-matter`, returns `HoleData[]` sorted by number
     - Export `const holes: HoleData[] = loadHoles()` for convenience import
     - This file uses Node.js `fs` -- it can ONLY be imported in server components or build-time code. NEVER import in "use client" files.

  4. Refactor `score/src/app/page.tsx`:
     - REMOVE the "use client" directive (make it a server component)
     - Import `holes` from `@/lib/holes`
     - Pass `holesData={holes}` prop to `<AppShell />`

  5. Update `score/src/components/app-shell.tsx`:
     - Add `HoleData` import from `@/lib/holes` (type-only import: `import type { HoleData } from "@/lib/holes"`)
     - Change `AppShell` to accept `{ holesData: HoleData[] }` prop
     - Keep all existing functionality (hydration guard, back-button, AnimatePresence)
     - Remove the inline `LandingScreen` and `SetupScreen` functions (they'll be separate components in Task 2)
     - Keep inline `ScoringScreen` and `ResultsScreen` as placeholders for now, but pass `holesData` to ScoringScreen placeholder
     - Import and render `<LandingScreen />` and `<SetupScreen />` from their new files (created in Tasks 2 and 3)
  </action>
  <verify>
    <automated>cd /Users/chriskruki/gitlab/playground-hb/score && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>9 hole markdown files exist with correct frontmatter. holes.ts exports HoleData type and loads all 9 holes. page.tsx is a server component passing holesData to AppShell. AppShell accepts holesData prop. Build succeeds with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Build branded landing screen with logo and Start Game button</name>
  <files>score/src/components/landing-screen.tsx</files>
  <action>
  Create `score/src/components/landing-screen.tsx` as a "use client" component:

  - Import `Logo` from `@/components/logo`, `Button` from `@/components/ui/button`, `useGameStore` from `@/lib/game-store`, `motion` from `framer-motion`
  - The component renders a centered, inviting landing page:
    - Logo component at the top (already handles the brand image)
    - Course name text: "Playground HB" in large text (text-3xl font-bold text-primary) and "Mini Golf Scorecard" as subtitle (text-lg text-muted-foreground)
    - Generous vertical spacing between elements (use flex flex-col items-center gap-6, with py-12 or similar for vertical breathing room)
    - A prominent "Start Game" Button (size="lg", full width, className="w-full text-lg") that calls `goToSetup()`
    - Subtle entrance animation: the entire content fades in and slides up slightly on mount using motion.div (initial={{ opacity: 0, y: 20 }}, animate={{ opacity: 1, y: 0 }})
  - Do NOT wrap in a Card -- the landing should feel open and welcoming, not boxed
  - Export as named export: `export function LandingScreen()`
  </action>
  <verify>
    <automated>cd /Users/chriskruki/gitlab/playground-hb/score && npx tsc --noEmit</automated>
  </verify>
  <done>LandingScreen component shows Playground HB branding with logo, course name, and a Start Game button that transitions to setup phase.</done>
</task>

<task type="auto">
  <name>Task 3: Build animated player setup screen with add/remove and validation</name>
  <files>score/src/components/setup-screen.tsx</files>
  <action>
  Create `score/src/components/setup-screen.tsx` as a "use client" component:

  - Import: `useState` from react, `useGameStore` from game-store, `Button` from ui/button, `Input` from ui/input, `Card/CardContent` from ui/card, `motion/AnimatePresence` from framer-motion, `Plus/Trash2/ChevronRight` from lucide-react

  **Layout:**
  - Header section: "Who's Playing?" title (text-2xl font-bold text-center)
  - Player input row: `Input` (placeholder "Enter player name", autoComplete="off") + Add button (Button variant="outline" size="icon" with Plus icon). On Enter key or Add button click: if input is non-empty, call `addPlayer(inputValue.trim())`, clear input, refocus input.
  - Player list: AnimatePresence list of current players. Each player row is a `motion.div` with layout animation (initial={{ opacity: 0, height: 0 }}, animate={{ opacity: 1, height: "auto" }}, exit={{ opacity: 0, height: 0 }}). Each row shows:
    - Player number (index + 1) as a small circle with bg-primary text-primary-foreground
    - Player name (editable -- use an Input with the current name, onChange calls `updatePlayerName(id, newName)`)
    - Trash2 icon button (Button variant="ghost" size="icon") to call `removePlayer(id)`
  - Start Scoring button at the bottom: Button (size="lg", full width, with ChevronRight icon) calling `goToScoring()`
    - DISABLED when: players.length === 0 OR any player has an empty/whitespace-only name
    - Show helper text below when disabled: "Add at least one player to continue" (text-sm text-muted-foreground)

  **Validation logic (SETUP-04):**
  - The goToScoring() store action already guards against zero/empty-name players, but the UI should ALSO disable the button and show feedback so users understand why they can't proceed
  - `const canStart = players.length > 0 && players.every(p => p.name.trim().length > 0)`
  - Button disabled={!canStart}

  **Animation (SETUP-03):**
  - Player list items animate in/out via AnimatePresence
  - Use `layout` prop on motion.div for smooth reorder when items are removed
  - Transition duration: 0.2s for snappy feel

  Export as named export: `export function SetupScreen()`
  </action>
  <verify>
    <automated>cd /Users/chriskruki/gitlab/playground-hb/score && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>SetupScreen allows adding/removing players with animated list, editable names, validation preventing start with zero/empty players, and a disabled state with helper text. Full build succeeds.</done>
</task>

</tasks>

<verification>
1. `cd score && npm run build` -- zero errors, successful build
2. `ls score/src/content/holes/hole-*.md | wc -l` -- returns 9
3. `grep -l "use client" score/src/app/page.tsx` -- should return nothing (page.tsx is server component)
4. `grep "holesData" score/src/app/page.tsx` -- confirms prop passing
5. `grep "holesData" score/src/components/app-shell.tsx` -- confirms prop reception
6. `grep "LandingScreen" score/src/components/landing-screen.tsx` -- confirms export
7. `grep "SetupScreen" score/src/components/setup-screen.tsx` -- confirms export
</verification>

<success_criteria>
- All 9 hole markdown files exist with correct frontmatter (name, par, illustration) and instruction text
- holes.ts exports HoleData interface and loads all 9 holes via gray-matter at server-side
- page.tsx is a server component (no "use client") that passes holesData to AppShell
- AppShell accepts holesData prop and renders imported LandingScreen and SetupScreen
- LandingScreen shows logo, branding text, and Start Game button
- SetupScreen supports add/remove players, editable names, animated list, and validation
- `npm run build` passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-game-flow/02-01-SUMMARY.md`
</output>
