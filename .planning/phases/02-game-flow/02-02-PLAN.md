---
phase: 02-game-flow
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - score/src/components/scoring-screen.tsx
  - score/src/components/score-input.tsx
  - score/src/components/player-score-row.tsx
  - score/src/components/hole-progress.tsx
  - score/src/components/app-shell.tsx
autonomous: true
requirements: [SCORE-01, SCORE-02, SCORE-03, SCORE-04, SCORE-05, SCORE-06, SCORE-07, SCORE-08, HOLE-01]

must_haves:
  truths:
    - "User sees one hole at a time with hole name, number, par value, instructions, and illustration placeholder"
    - "User enters scores for all players via +/- buttons defaulting to par"
    - "User sees color-coded over/under par indicator per player (green under, red over)"
    - "User sees running total strokes per player throughout all holes"
    - "User sees hole progress indicator (Hole X / 9)"
    - "User experiences smooth animated transitions between holes"
    - "After hole 9, user can advance to results"
  artifacts:
    - path: "score/src/components/scoring-screen.tsx"
      provides: "Complete scoring screen with hole content, player scores, progress, transitions"
      exports: ["ScoringScreen"]
    - path: "score/src/components/score-input.tsx"
      provides: "+/- score input component with par default"
      exports: ["ScoreInput"]
    - path: "score/src/components/player-score-row.tsx"
      provides: "Single player row with score input and running total"
      exports: ["PlayerScoreRow"]
    - path: "score/src/components/hole-progress.tsx"
      provides: "Hole progress indicator (X / 9)"
      exports: ["HoleProgress"]
  key_links:
    - from: "score/src/components/scoring-screen.tsx"
      to: "score/src/lib/game-store.ts"
      via: "reads currentHole, players, scores; calls setScore, nextHole, goToResults"
      pattern: "useGameStore"
    - from: "score/src/components/scoring-screen.tsx"
      to: "HoleData[]"
      via: "receives holesData prop from AppShell"
      pattern: "holesData\\[currentHole"
    - from: "score/src/components/score-input.tsx"
      to: "score/src/components/scoring-screen.tsx"
      via: "receives score/par/onChange, renders +/- buttons"
      pattern: "ScoreInput"
    - from: "score/src/components/app-shell.tsx"
      to: "score/src/components/scoring-screen.tsx"
      via: "passes holesData to ScoringScreen"
      pattern: "ScoringScreen.*holesData"
---

<objective>
Build the complete scoring screen with hole content display, per-player +/- score inputs, par tracking, running totals, progress indicator, and animated hole transitions.

Purpose: This is the core gameplay loop -- the screen where players spend 90% of their time. It must show hole-specific content from markdown data, allow score entry for every player on each hole, provide par-relative feedback, and animate smoothly between holes.

Output: ScoringScreen, ScoreInput, PlayerScoreRow, and HoleProgress components. Updated AppShell wiring.
</objective>

<execution_context>
@/Users/chriskruki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskruki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-game-flow/02-RESEARCH.md
@.planning/phases/02-game-flow/02-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan depends on -->

From score/src/lib/holes.ts (created in Plan 01):
```typescript
export interface HoleData {
  number: number;
  name: string;
  par: number;
  illustration: string;
  instructions: string;
}
export const holes: HoleData[];
```

From score/src/lib/game-store.ts:
```typescript
export type GamePhase = "landing" | "setup" | "scoring" | "results";
export interface Player { id: string; name: string; }

// Scoring-relevant store shape:
interface GameState {
  phase: GamePhase;
  players: Player[];
  currentHole: number;          // 1-indexed (1 through 9)
  scores: Record<string, number[]>;  // playerId -> array of scores per hole (0-indexed)

  setScore: (playerId: string, hole: number, score: number) => void;  // hole is 1-indexed
  nextHole: () => void;         // increments currentHole if < 9
  prevHole: () => void;         // decrements currentHole if > 1
  goToResults: () => void;      // scoring -> results (guards phase === "scoring")
}
```

From score/src/components/app-shell.tsx (after Plan 01 refactor):
```typescript
// AppShell accepts holesData prop and renders ScoringScreen when phase === "scoring"
// ScoringScreen will receive holesData as a prop from AppShell
export function AppShell({ holesData }: { holesData: HoleData[] }): JSX.Element;
```

From score/src/components/ui/button.tsx:
```typescript
// Variants: default, destructive, outline, secondary, ghost, link
// Sizes: default, sm, lg, icon (all min-h-[44px] for touch)
export { Button };
```

Theme colors (from globals.css):
- Primary: #34654A (forest green) -- used for primary actions
- Background: #FFF2D4 (warm yellow)
- Destructive: oklch(0.577 0.245 27.325) -- red, use for over-par
- For under-par green: use "text-green-600" (Tailwind built-in, distinct from primary green)
- For over-par red: use "text-red-500" (Tailwind built-in)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create score input, player row, progress components, and full scoring screen</name>
  <files>score/src/components/score-input.tsx, score/src/components/player-score-row.tsx, score/src/components/hole-progress.tsx, score/src/components/scoring-screen.tsx</files>
  <action>
  Create 4 components, all "use client":

  **1. score/src/components/hole-progress.tsx (SCORE-07)**
  - Props: `{ current: number; total: number }` (e.g., current=4, total=9)
  - Renders a progress indicator: "Hole {current} / {total}" text + a visual progress bar
  - Progress bar: a row of 9 small circles or a thin bar with filled portion. Use `bg-primary` for completed holes, `bg-muted` for upcoming. Keep it compact.
  - Export: `export function HoleProgress({ current, total }: { current: number; total: number })`

  **2. score/src/components/score-input.tsx (SCORE-03)**
  - Props: `{ value: number; par: number; onChange: (score: number) => void; min?: number }`
  - Renders a horizontal row: [ - button ] [ score display ] [ + button ]
  - Minus button: Button variant="outline" size="icon" with Minus icon from lucide-react. Disabled when value <= (min ?? 1).
  - Plus button: Button variant="outline" size="icon" with Plus icon from lucide-react.
  - Score display in center: large text (text-2xl font-bold) showing the current value. Apply par color: `value < par ? "text-green-600" : value > par ? "text-red-500" : "text-foreground"`
  - Min score is 1 (cannot score 0 in mini golf). Default min=1.
  - Export: `export function ScoreInput({ value, par, onChange, min = 1 }: ScoreInputProps)`

  **3. score/src/components/player-score-row.tsx (SCORE-02, SCORE-05, SCORE-06)**
  - Props: `{ player: Player; hole: number; par: number; score: number; totalStrokes: number; onScoreChange: (score: number) => void }`
  - Renders a Card (or styled div) for one player on the current hole:
    - Player name (font-medium)
    - ScoreInput component with value=score, par=par, onChange=onScoreChange
    - Par indicator text: show "+N" (over, text-red-500) or "-N" (under, text-green-600) or "E" (even, text-muted-foreground) relative to par for this hole
    - Running total: "Total: {totalStrokes}" (text-sm text-muted-foreground)
  - Export: `export function PlayerScoreRow(props: PlayerScoreRowProps)`

  **4. score/src/components/scoring-screen.tsx (SCORE-01, SCORE-04, SCORE-08, HOLE-01)**
  - Props: `{ holesData: HoleData[] }`
  - Import useGameStore for: currentHole, players, scores, setScore, nextHole, prevHole, goToResults
  - Get current hole data: `const hole = holesData[currentHole - 1]` (currentHole is 1-indexed)
  - Track transition direction in local state: `const [direction, setDirection] = useState<1 | -1>(1)`

  **Hole content area (HOLE-01, SCORE-01, SCORE-04):**
  - HoleProgress at the top
  - Hole name and number: "Hole {hole.number}: {hole.name}" (text-xl font-bold)
  - Par display: "Par {hole.par}" in a small badge/pill (bg-primary/10 text-primary rounded-full px-3 py-1)
  - Illustration placeholder: a rounded div (h-32 bg-muted rounded-lg) with a golf flag icon (Flag from lucide-react) centered and the hole number. No actual SVG illustration files needed yet.
  - Instructions text: hole.instructions in text-sm text-muted-foreground

  **Player scores area (SCORE-02, SCORE-03, SCORE-05, SCORE-06):**
  - For each player, render a PlayerScoreRow
  - Score for current hole: `scores[player.id]?.[currentHole - 1] ?? hole.par` (default to par if not yet set, per research Pitfall 2)
  - Running total: sum of all scores in `scores[player.id]` array, treating undefined entries as the par for that hole (so total is always accurate even for unvisited holes)
  - onScoreChange calls `setScore(player.id, currentHole, newScore)`

  **Navigation (SCORE-08):**
  - "Previous Hole" button (Button variant="outline", only shown when currentHole > 1). onClick: `setDirection(-1)` then `prevHole()`
  - "Next Hole" button (Button variant="default"). onClick: first auto-fill any missing scores for current hole with par value (loop through players, if `scores[player.id]?.[currentHole - 1]` is undefined, call `setScore(player.id, currentHole, hole.par)`), then `setDirection(1)` then `nextHole()`
  - On hole 9: "Next Hole" becomes "View Results" (Button variant="default" with secondary styling). Same auto-fill logic, then call `goToResults()`

  **Animated transitions (SCORE-08):**
  - Wrap the entire hole content + player scores in `AnimatePresence mode="wait"` with a `motion.div key={currentHole}`
  - Use directional slide variants:
    ```
    enter: (dir) => ({ x: dir > 0 ? 300 : -300, opacity: 0 })
    center: { x: 0, opacity: 1 }
    exit: (dir) => ({ x: dir > 0 ? -300 : 300, opacity: 0 })
    ```
  - Transition: `{ duration: 0.25, ease: "easeInOut" }`
  - Pass `custom={direction}` to motion.div and AnimatePresence

  Export: `export function ScoringScreen({ holesData }: { holesData: HoleData[] })`
  </action>
  <verify>
    <automated>cd /Users/chriskruki/gitlab/playground-hb/score && npx tsc --noEmit</automated>
  </verify>
  <done>All 4 scoring components exist with correct types. ScoreInput has +/- buttons with par coloring. PlayerScoreRow shows name, score input, par indicator, and running total. HoleProgress shows hole X/9. ScoringScreen composes everything with hole content, all player scores, and directional AnimatePresence transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ScoringScreen into AppShell and verify full game flow builds</name>
  <files>score/src/components/app-shell.tsx</files>
  <action>
  Update `score/src/components/app-shell.tsx`:

  1. Import `ScoringScreen` from `@/components/scoring-screen`
  2. Replace the inline `ScoringScreen` placeholder function with the imported component
  3. In the render, pass holesData: `<ScoringScreen holesData={holesData} />`
  4. Keep the inline `ResultsScreen` placeholder as-is (Phase 3 handles it)
  5. Ensure the AnimatePresence wrapping still uses `key={phase}` for phase transitions (landing/setup/scoring/results), and the ScoringScreen internally uses `key={currentHole}` for hole transitions

  Verify the complete game flow works end-to-end:
  - Landing -> tap Start Game -> Setup
  - Setup -> add players -> tap Start Scoring -> Scoring (hole 1)
  - Scoring -> enter scores -> Next Hole (repeat through hole 9)
  - Hole 9 -> View Results -> Results placeholder

  Run full build to confirm no type errors, no "fs" leaking into client bundle, and all imports resolve.
  </action>
  <verify>
    <automated>cd /Users/chriskruki/gitlab/playground-hb/score && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>ScoringScreen is wired into AppShell with holesData prop. Full game flow compiles: landing -> setup -> scoring (all 9 holes with transitions) -> results. Build succeeds with zero errors and no "fs" module leakage into client bundle.</done>
</task>

</tasks>

<verification>
1. `cd score && npm run build` -- zero errors, successful production build
2. `grep "ScoringScreen" score/src/components/app-shell.tsx` -- imported and rendered
3. `grep "holesData" score/src/components/scoring-screen.tsx` -- receives hole data prop
4. `grep "ScoreInput" score/src/components/scoring-screen.tsx` -- uses score input component
5. `grep "AnimatePresence" score/src/components/scoring-screen.tsx` -- has hole transitions
6. `grep "text-green-600\|text-red-500" score/src/components/score-input.tsx` -- has par coloring
7. No "use client" files import from `@/lib/holes` directly (only type imports allowed)
</verification>

<success_criteria>
- User sees one hole at a time with name, number, par, instructions, and illustration placeholder
- Score input uses +/- buttons with par as default starting value, minimum score 1
- Over/under par is color-coded: green for under, red for over, neutral for even
- Running total per player is displayed and accurate across all holes
- Hole progress shows "Hole X / 9"
- Holes transition with directional slide animation (forward slides left, backward slides right)
- Auto-fills missing scores with par when advancing to prevent NaN totals
- After hole 9, "View Results" advances to results phase
- `npm run build` succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-game-flow/02-02-SUMMARY.md`
</output>
